# RapidEngine — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# HTTP client (Ollama proxy + OpenAI SDK)
httpx>=0.27.0
openai>=1.30.0

# PDF extraction
PyPDF2>=3.0.1

# XLSX extraction
openpyxl>=3.1.2

# DOCX extraction (optional graceful fallback)
python-docx>=1.1.0

# Research agent — DuckDuckGo (no API key required)
duckduckgo-search>=6.1.7

# NLP / FinBERT sentiment
transformers>=4.41.0
torch>=2.2.0
sentencepiece>=0.2.0

# Pydantic v2 (FastAPI default)
pydantic>=2.7.0